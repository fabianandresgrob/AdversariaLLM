defaults:
  - models: models
  - datasets: datasets
  - splits
  - paths
  - _self_

model: meta-llama/Meta-Llama-3.1-8B-Instruct
chat_template_id: meta-llama/Llama-3.1-8B-Instruct

hard_benign_sources: [or_bench]     # xs_test is NEVER a training source (held out for eval)
max_new_tokens: 256
seed: 0
out_path: ${root_dir}/data/benign_targets/or_bench_ygen.json
